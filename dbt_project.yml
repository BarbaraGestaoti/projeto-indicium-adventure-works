name: 'projeto_indicium_adventure_works'
version: '1.0.0'

profile: 'projeto_indicium_adventure_works'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  projeto_indicium_adventure_works:
    +catalog: fea_academy
    staging:
      +materialized: view
    marts:
      +materialized: table